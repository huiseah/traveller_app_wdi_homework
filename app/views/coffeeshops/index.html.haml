%script
  var coffeeshops = [];
%ol
  - @coffeeshops.each do |coffeeshop|
    %script
      = "$(document).ready(function () {"
      = "add_marker(#{coffeeshop.latitude}, #{coffeeshop.longitude}, '#{coffeeshop.name}');"
      = "});"
%div#map
  %div#map_canvas
